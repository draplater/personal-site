{
  "name": "personal-site",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && cp workers/_worker.js dist/_worker.js",
    "preview": "npm run build && wrangler pages dev ./dist",
    "deploy": "npm run build && wrangler pages deploy ./dist"
  },
  "dependencies": {
    "astro": "^4.0.0"
  },
  "devDependencies": {
  }
}
